# Codex Install Notes

This repository is structured as a root-level Codex plugin.

Plugin manifest:

```text
.codex-plugin/plugin.json
```

Primary skill:

```text
skills/a-share-investment-research/SKILL.md
```

Local validation:

```bash
python ~/.codex/skills/.system/plugin-creator/scripts/validate_plugin.py .
```
