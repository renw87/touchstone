# Hooks

Reserved for future session hooks.

Planned hooks:

- session start: remind agent to read `CODEX.md` and signal boundary.
- pre-run: validate `input.json`.
- post-run: validate `signal.json` and write `evaluation.json`.
